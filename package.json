{
    "name": "imposter-game",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "bun --filter '*' dev",
        "build": "bun --filter '*' build",
        "lint": "bun --filter '*' lint",
        "typecheck": "bun --filter '*' typecheck",
        "dev:server": "bun --filter @imposter/server dev",
        "dev:frontend": "bun --filter @imposter/frontend dev"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}